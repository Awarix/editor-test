import { Typography } from '@material-ui/core'
import React from 'react'

const AudioPlayer = () => {
  return (
    <div style={{display: 'flex', width: '100%', height: '250px', background: '#fafafa', textAlign: 'center', justifyContent: 'center', alignItems: 'center'}}>
    <Typography variant='h2'>
      AudioPlayer
    </Typography>  
    </div>
  )
}

export default AudioPlayer